#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;
// O nlog(n)
class Solution {
public:
    void sortColors(vector<int>& nums) {
        sort(nums.begin() , nums.end());
    }
};
int main ()
{
    
    return 0;
}
